# React Todo App

A simple and responsive Todo App built using **React.js**.  
Users can add, edit, mark complete, and delete tasks — all stored locally using the browser’s localStorage (no backend required).

## 🧠 Features
- ➕ Add new tasks
- ✅ Mark tasks as complete
- 📝 Edit existing tasks
- ❌ Delete tasks
- 🔍 Filter tasks (All / Active / Done)
- 💾 Automatically saves to localStorage

## 🧰 Built With
- React.js (Create React App)
- CSS for styling
- LocalStorage for persistence

## 🚀 How to Run
```bash
git clone https://github.com/YOUR_USERNAME/react-todo-app.git
cd react-todo-app
npm install
npm start
